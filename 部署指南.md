# Eraser · 部署上线指南

现在你本地跑的是 `http://localhost:8000`，**这不是公网网址**——它只是你自己电脑上的一个临时服务，别人访问不到，电脑关机也打不开。

要让任何人都能访问，需要把代码放到一台**持续在线的服务器**上并绑定一个域名。这类服务叫「静态网站托管」，对个人项目免费。

本项目的代码已经 **100% 准备就绪**（无构建步骤、纯静态，约 150KB），下面三个任选一个即可。

---

## 零、部署前先做本地自检（别跳过）

在推到线上之前，先在**真正的 Chrome 浏览器**里完整跑一遍：

```bash
cd /Users/zhaoziqi/Desktop/消除水印
python3 -m http.server 8000
```

打开 `http://localhost:8000`，走完这条链路：

选图 → 图片显示出来 → 涂抹标记 → 点「开始去除」→ 出结果 → 文件出现在 `~/Downloads`

⚠️ **不要用 IDE 的内置预览窗口测试**。它通常不支持文件下载，会让你误以为导出功能坏了——实际是正常的。

### 需要上传的文件

```
index.html          必须
favicon.svg         必须（浏览器标签页图标）
css/style.css       必须
js/*.js             必须（7 个文件）
README.md           可选
部署指南.md          可选
.gitignore          可选
```

### 绝对不要上传

- **`.codebuddy/`** —— 本地工具数据目录，含你的会话与项目记录
- **`.DS_Store`** —— macOS 自动生成的系统文件

用 **Git 方式**时 `.gitignore` 会自动排除这两项。但 **Vercel / Netlify 的拖拽上传不会读 `.gitignore`**，拖拽前请先手动删掉：

```bash
cd /Users/zhaoziqi/Desktop/消除水印
rm -rf .codebuddy .DS_Store
```

---

## 一、三种方案怎么选

| 方案 | 上线所需时间 | 难度 | 网址格式 | 适合 |
| --- | --- | --- | --- | --- |
| **Vercel** | **约 1 分钟** | 最简单，拖拽即可 | `eraser-xxx.vercel.app` | 想立刻拥有一个能用、能分享的网址 |
| **GitHub Pages** | 约 5 分钟 | 中等，需会 git | `用户名.github.io/仓库名` | 想和代码存档绑一起，长期维护 |
| **Netlify** | 约 2 分钟 | 简单 | `xxx.netlify.app` | 介于两者之间 |

> **建议**：第一次上线先用 **Vercel**，一分钟拿到网址验证效果；确认没问题后，再决定要不要走 GitHub Pages 做正式版本。两条路不冲突。

三种方案的**共同点**：自动 HTTPS、免费、都支持后续一键更新。由于 Eraser 的图片处理全在浏览器本地完成，上线后你依然不需要为「服务器处理图片」付一分钱。

---

## 二、方案 A：GitHub Pages（正式版）

### 第 1 步：准备 GitHub 账号

访问 <https://github.com> ，点右上角 **Sign up** 注册（有账号则直接 Sign in）。记住你的**用户名**，网址里会用到。

### 第 2 步：创建仓库

1. 登录后右上角点 **＋** → **New repository**
2. **Repository name** 填 `eraser`
3. 选 **Public**（必须公开，私有仓库的 Pages 需要付费）
4. ⚠️ **不要**勾选 "Add a README file" 等任何初始化选项——本地已经有 `.gitignore` 了，勾选会造成冲突
5. 点 **Create repository**

创建完成后会跳到一个空白页，页面上有一大段命令，**先别关**，下一步要用。

### 第 3 步：把代码传上去

打开终端，执行（把 `<你的用户名>` 换成实际用户名，比如 `zhaoziqi`）：

```bash
cd /Users/zhaoziqi/Desktop/消除水印

git init
git add .
git commit -m "Eraser: 图片水印处理工具首个版本"
git branch -M main
git remote add origin https://github.com/<你的用户名>/eraser.git
git push -u origin main
```

第一次 push 会弹窗要求登录 GitHub，按提示输入账号密码即可。

> 如果系统提示找不到 git，先执行 `xcode-select --install` 安装。

### 第 4 步：开启 Pages

1. 回到 GitHub 仓库页面，点顶部标签栏最右侧的 **Settings**
2. 左侧菜单点 **Pages**
3. **Build and deployment** → **Source** 下拉选 **Deploy from a branch**
4. **Branch** 选 `main`，右侧文件夹选 `/ (root)`
5. 点 **Save**

### 第 5 步：拿到网址

保存后页面顶部会出现提示：

> Your site is live at **https://<你的用户名>.github.io/eraser/**

首次生效需要 **1～3 分钟**（期间访问是 404，正常）。看到 🎉 之后把这个网址发给任何人都能直接打开。

---

## 三、方案 B：Vercel（最快，推荐先试）

1. 访问 <https://vercel.com>，点 **Sign Up**，选 **Continue with GitHub** 用 GitHub 账号登录
2. 登录后点 **Add New → Project**
3. 如果你的代码已在 GitHub：在 Import 列表找到 `eraser` 仓库点 **Import**（推荐，能自动更新）
   - 如果还没推到 GitHub：直接把本地整个 `消除水印` 文件夹**拖拽**到页面上
     ⚠️ 拖拽前务必先执行第零章的清理命令删掉 `.codebuddy` 和 `.DS_Store`——拖拽上传不会读 `.gitignore`
4. 配置页保持默认即可，**不要**改 Framework Preset（选 Other），也不要填 Build Command——本项目无需构建
5. 点 **Deploy**
6. 约 30 秒后出现 🎉 页面，网址类似 `https://eraser-abc123.vercel.app`

**以后要更新**：在终端执行 `npx vercel --prod`（同一目录）即自动重新部署。

---

## 四、方案 C：Netlify（拖拽即可）

1. 访问 <https://app.netlify.com/drop>，直接用 GitHub 登录
2. 把本地 `消除水印` 文件夹整个拖到页面上的虚线框里
   ⚠️ 拖拽前务必先执行第零章的清理命令删掉 `.codebuddy` 和 `.DS_Store`——拖拽上传不会读 `.gitignore`
3. 等待上传完成，自动生成网址 `https://xxxx.netlify.app`
4. 想改网址，`Site settings → Change site name`

---

## 五、上线后必做的验证清单

拿到网址后，请逐项确认：

- [ ] 网址开头是 `https://`（锁头图标），而非 `http://`
- [ ] 用**手机浏览器**打开，能正常显示
- [ ] 上传一张带水印的图，标记后点「快速修复」→ 能出结果（此步先验证基础流程）
- [ ] 切换到「AI 高质量」→ 浏览器自动开始下载模型（约 200MB，看进度条）
- [ ] 下载完成后去水印成功 → 关闭标签页重新打开，**模型应秒加载**（验证缓存生效）
- [ ] 把网址发给朋友，对方能打开

---

## 六、以后修改代码怎么更新网站

**GitHub Pages**：

```bash
cd /Users/zhaoziqi/Desktop/消除水印
git add .
git commit -m "说明这次改了什么"
git push
```

推送后等 1～2 分钟，刷新网址即为新版。

**Vercel / Netlify**：如果绑定了 GitHub 仓库，push 后会自动重新部署；如果是拖拽上传的，重新拖一次即可。

---

## 七、常见问题排查

**Q：访问网址显示 404**
Pages 首次部署需要 1～3 分钟；超过 5 分钟仍是 404 → Settings → Pages 确认 Source 选的是 `main` 分支且目录为 `/(root)`。

**Q：页面能打开，但一片空白 / 点了没反应**
按 `F12` 打开控制台看红色报错。最常见原因是**双击 html 文件**打开了而不是用 HTTP 服务——若网址是 `file://` 开头，说明部署方式不对，回到上面的流程重来。

**Q：模型下载到一半失败 / 卡住**
200MB 模型走的是 HuggingFace CDN，国内偶尔不稳。多试两次即可（已下载的部分会续传缓存）。实在不行可改用 Vercel/Netlify，或直接选用「快速本地修复」引擎，完全不依赖模型。

**Q：AI 引擎点「开始去除」后报错**
确认首次访问时是否完整下载过模型。另外**绝不**要删除 `js/inpaint.js` 中 `ort.env.wasm.numThreads = 1` 这一行——静态托管无法发送 COOP/COEP 头，去掉会导致 WASM 多线程初始化失败。

**Q：处理完却找不到下载的文件**
结果文件名是 `原文件名_无水印.png`，落在浏览器默认下载文件夹（macOS 为 `~/Downloads`）。Chrome 里按 **Cmd + Shift + J** 或访问 `chrome://downloads` 可看到实际路径。若是在 IDE 内置预览里操作的，那里不支持下载，请改用真正的 Chrome。

**Q：会不会很慢 / 要不要买服务器**
不需要。本项目无后端，图片永远不上传服务器，所有运算在访问者的浏览器里完成。你付的费用是 **0**。

---

## 八、两个可选优化

**自定义域名**：有了 `xxx.com` 后，在 Vercel/Netlify 的 Domains 设置里添加并按要求配置 DNS 解析，即可用自家域名访问。

**favicon 已内置**：项目根目录的 `favicon.svg` 就是品牌图标（四角取景框 + 橡皮），`index.html` 已挂好：

```html
<link rel="icon" type="image/svg+xml" href="favicon.svg">
```

换图标只需替换该文件（保持同名、24×24 viewBox）。若嫌它在 14px 的标签页上偏糊，可另存一份加粗简化版并在 link 里改指向。
